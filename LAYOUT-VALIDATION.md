# 布局与原生顶栏验证 · 2026-09-25

- 缩放输入按动画帧合并，结束时提交最后一个位置；计算使用大板内容宽度与 4px 间距。
- 布局过渡只动画 transform，包含横纵位移与宽高比例，不逐帧动画 height；新操作取消旧任务，缩放开始取消尚在运行的区块过渡。
- 溢出检查分批读取、写入；窗口变化保留 DOM，仅重算可见卡片。移除无视觉效果的鼠标高光监听。
- 毛玻璃由类目统一处理，卡片保留同色透明底与阴影，移除重复的嵌套实时模糊。
- 原生 NSToolbar 融合系统三色窗口按钮，搜索使用 NSSearchField，操作使用 NSButton。Mac 隐藏重复网页工具栏，浏览器保留网页工具栏。菜单提供 Command-K 原生搜索。

`tests/layout.playwright.js` 在不保存的独立测试页检查：拖动缩放 80px 的误差小于 1px、窗口变化保持 DOM、拖动排序、内部卡片紧凑、减少动态效果偏好、390px 宽无横向溢出。全部通过。

本机浏览器 8 次折叠切换采样：修改前帧间隔 P95 约 41.6ms，最终采样约 25ms，最大约 33.1ms。不是统一硬件基准，不能证明原生 WKWebView 或所有设备恒定帧率；仍有慢帧。验证页禁用缓存以避免旧脚本污染结果。

原生实测：搜索 Figma 过滤成功、清空恢复、布局切换显示/隐藏手柄、添加按钮打开弹窗。构建与本地存储测试通过。原生界面通过辅助功能树确认是 NSToolbar/NSSearchField/NSButton；画板本体仍使用 WKWebView。
