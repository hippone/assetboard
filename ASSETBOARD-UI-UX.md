# Assetboard UI / UX 研究与设计依据

2026-09-24 · 研究优先级：最高 · 状态：文献与交互模式研究完成第一轮；未做用户测试或产品原型实测。

**最新方向**：大板按类别添加区块，区块内放资产卡片；区块自适应、有最小尺寸且可手动放大缩小，编辑与查看 UI 差异较小。详见 [大板与轻编辑设计](ASSETBOARD-BOARD-DESIGN.md)。下文三种互斥首页的比较保留为研究历史；当前转为同一板上不同类别区块的内容组织。

## 1. 产品质量首先由使用体验定义

用户明确强调：展示和管理是产品核心，UI / UX 是重中之重。

最新强调：**图形化才是最重要的**。后续视觉原型以图形识别、空间组织和简洁状态图形为主，短文字提供准确含义。现有字段表和文字草图是内容说明，不是最终界面模板。

本轮调整：不再提前把“项目资产架”当成确定首页。它是候选，必须与资产目录和常用入口视角比较。连接器和数据模型服务于体验需求，不能反过来决定信息架构。

评价一个设计的顺序：能理解 → 能找到 → 能判断 → 能操作 → 能恢复 → 愿意长期使用。视觉品质贯穿每一步。

本文件中的尺寸、时长和布局均为原型起点或验证目标，不是已证实的最优值。

## 2. 原始资料支持什么，不能支持什么

| 一手来源 | 可借鉴原则或官方行为 | 转化为 Assetboard 的设计假设 |
|---|---|---|
| [NN/g：识别与回忆](https://www.nngroup.com/articles/recognition-and-recall/) | 可见线索和上下文有助于识别，减少记忆负担 | 名称旁保留用途、平台、账号线索；不只显示 logo 或机器生成 ID |
| [NN/g：渐进披露](https://www.nngroup.com/articles/progressive-disclosure/) | 高频基础信息先出现，复杂次要信息按需展开 | 首页给身份和下一事件，配置与来源细节在详情；关键操作不能藏到难以发现 |
| [Carbon：Data table](https://carbondesignsystem.com/components/data-table/usage/) | 支持搜索、排序、展开、选择和批量操作，操作目标应明确 | 将阅读和选中后的管理状态区分；给予清单充分宽度，避免嵌套表格 |
| [Linear：Display options](https://linear.app/docs/display-options) | 视图可调整呈现、分组及显示属性 | 同一份资产支持不同观察方式；默认值承担主要设计责任 |
| [Linear：Custom views](https://linear.app/docs/custom-views) | 过滤后的工作集合可以保存成视图 | 用户反复查“域名/近期续费”时可保存条件，而非复制资产 |
| [W3C：目标尺寸](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) | WCAG 2.2 AA 对指针目标尺寸/间距设要求，存在例外 | 原型优先用宽裕的触达区域；小图标不等于小点击区 |
| [W3C：焦点不被遮挡](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) | 键盘焦点元素不能被作者创建内容完全遮挡 | 底部批量栏、固定头部和详情面板必须检查焦点可见性 |

这些资料支持设计原则，不证明本产品的用户偏好。Linear 的能力是参照，不意味着我们需要完整复制其配置复杂度。此前 Homepage、Glance、Wallos 继续作为产品参照；尚无截图逐项审计或竞品任务实测。

## 3. 从任务而不是页面菜单出发

| 核心任务 | 用户可能记得的线索 | 必须看到的内容 | 达成标志 |
|---|---|---|---|
| 找到管理入口 | 域名、昵称、平台或项目 | 资产身份、账号、控制台链接 | 打开正确对象对应的入口 |
| 理解资产用途 | 一笔商户扣款或陌生名称 | 用途、关联项目、保留原因 | 能解释为什么还留着 |
| 判断近期事项 | 日期模糊或担心忘记 | 事件类型、明确日期、自动续费情况 | 知道是否需要行动及去哪里处理 |
| 收下新资产 | 购买链接、一句话 | 最少输入、保存反馈 | 记录已保存，可随后找回 |
| 整理多项资产 | 平台或类型 | 选择范围、批量动作、结果 | 归属改变且能撤销 |
| 重新打开旧项目 | 项目名称或用途 | 依赖、共享资源、上次备注 | 找到所需资源，不误动共享项 |

首页必须支持“只记得一点点”的寻找。假如必须先知道确切项目，才能找到资产，就重建了用户原本的记忆负担。

## 4. 信息架构候选：先比较，再决定

| 候选 | 默认组织 | 擅长 | 潜在问题 | 否决信号 |
|---|---|---|---|---|
| A 项目分组 | 项目下的紧凑资源行 | 理解用途和项目依赖 | 需要已有归属；共享工具不好放 | 找域名时频繁逐个展开项目 |
| B 资产目录 | 名称、平台/账号、用途、下一事件 | 查找、对比、批量整理 | 容易变得冷硬、列太多 | 用户需反复横向扫读才知道是什么 |
| C 常用入口 + 全部资产 | 少量置顶入口加完整目录 | 日常访问频繁的平台 | 不常用资产容易被遗忘 | 用户误以为置顶部分就是全部资产 |

关系地图保留在项目详情中，暂不作为首页候选；它主要服务于理解依赖。

比较时使用相同内容、字体、颜色和资产数量，先观察结构差异。之后再在胜出的结构上比较视觉方向，避免把“更喜欢颜色”误判为“更容易用”。

导航初稿：总览 / 资产 / 近期事项。项目可先作为分组与筛选维度；若验证发现项目恢复任务足够常见，再设独立入口。用户自定义视图等实际重复使用后再出现。

## 5. 一览与管理的两种状态

### 日常查看

- 默认无选中状态，名称、用途、平台及下一事件清晰对齐。
- 不要求进入正式的“管理模式”才能编辑一条资料；查看详情即可修改。
- 多选入口可发现，但批量工具栏只在选中后出现。
- 主入口明确；打开详情与跳去平台分别有自己的点击目标。
- 首页不因同步或用户正在阅读时的排序变化而突然重排。

### 整理与管理

- 选中后显示“已选 3 项”，只展示适用于这批对象的动作。
- “全选”明确指当前页、当前筛选结果还是全部记录，不能含糊。
- 跨筛选保留选择很容易误操作，原型默认切换筛选时清除选择并告知。
- 批量修改报告成功与失败项；失败项保留，支持重试。
- 可逆归属/标签编辑提供撤销；归档文案明确“从当前列表收起”。
- 存在未保存内容时，关闭详情提供保存/放弃/继续编辑；已保存则直接关闭。

## 6. UI 的具体研究对象

### 字体、对齐与密度

原型起点：正文 14–16px，项目标题 18–20px，数字采用便于对齐的形式。二级信息通过位置与字重区分，不通过过淡文字隐藏。所有字号仍需实屏检查。

比较 44–48px 的单行目录与 56–64px 的双行记录；这只是探索尺寸。触屏主要操作建议按约 44px 的触达区域设计，WCAG AA 的 24×24 CSS px 最低目标规则及其间距/例外需独立核对，不能混称同一标准。

名称与用途靠近；金额、日期与其单位完整显示；长名称优先保留能区分对象的部分，详情可查看并复制全称。不得依赖悬浮提示作为手机上唯一的信息来源。

### 视觉风格候选

- **安静的编辑式排版**：中性偏暖背景、明显的字级与组间留白、细分隔；看起来整洁且有生活感。风险是留白过多。
- **精确的工具式排版**：中性灰背景、稳定列对齐、更紧凑的行、少量色彩提示；适合高密度日常操作。风险是冷硬及术语过多。

两者都避免过度装饰：大 hero 区、无用途的趋势图、大面积渐变和每行不同的彩色标签不列为默认方案。明暗主题应在信息层级稳定后比较。

### 色彩和状态

颜色只辅助表达：到期、扣费、缺失和同步失败不能都变成红色“异常”。未知日期写“待补充”；部分更新写“部分来源未更新”；真实逾期才按优先级强调。

原型需检查文字对比、控件边界、焦点、色盲识别和减少动画偏好；遵循 [WCAG 2.2](https://www.w3.org/TR/WCAG22/) 的相应准则，不把某一组件库的通过结果当成本产品通过。

### 信息的稳定位置

不要采用高度差异很大的瀑布流作为资产主列表。它可以美观，但会让用户每次重新找位置，难以按列对比。首轮用固定栅格和分组行验证；项目折叠状态、搜索和返回位置要保留。

## 7. 必须设计的完整操作链

1. **找东西**：输入不完整线索 → 看到带用途的结果 → 打开详情 → 进入对应平台 → 回到原位置。无结果时显示当前筛选，允许清除条件或添加记录。
2. **记东西**：点添加 → 名称或链接 → 预览识别字段 → 保存 → 行内反馈并提供查看。不要让必填项目/费用/日期成为阻碍；失败保留输入。
3. **整理东西**：选择多条 → 选择项目或共享 → 预览影响数量 → 应用 → 结果反馈与撤销。合并重复项另设流程，保留字段冲突提示。
4. **处理日期**：看到事件 → 看来源及明确日期 → 去原平台 → 回来记录结果或稍后提醒。不可把“已读”当成“已续费”。
5. **改变用途**：打开详情 → 修改用途/归属 → 保存反馈 → 原列表同步更新。同步数据与用户注释分开，不让平台刷新覆盖人的理解。

侧栏用于简短查看与编辑；内容复杂时可打开独立详情。桌面关闭后焦点回到原条目，手机用完整页面并保留返回位置。外部链接、删除、归档、关闭面板使用不同且明确的名称。

## 8. 状态矩阵是交付的一部分

| 场景 | 用户看到什么 | 可采取什么行动 |
|---|---|---|
| 初次空白 | 简短示例与添加入口；示例明确标识 | 添加自己的第一条、查看示例 |
| 筛选为空 | 条件仍可见，说明没有符合项 | 清除筛选；不是提示“还没有资产” |
| 加载中 | 保留结构和已有资料 | 继续看缓存，知道正在更新 |
| 部分来源失败 | 来源级提示、最后成功时间 | 重试或重新连接；旧资产不消失 |
| 金额/日期未知 | 清楚写未知或待补充 | 选择补充，不阻塞其他操作 |
| 保存失败 | 输入保留、具体失败反馈 | 重试；不误显示成功 |
| 多账号同名 | 平台与账号别名同时可辨认 | 选对对象和入口 |
| 归档但仍扣费 | 生命周期与费用状态分别显示 | 去平台处理，或恢复到列表 |
| 资产规模增大 | 稳定排序、搜索、收起分组 | 无需重新组织整个首页 |
| 键盘与触屏 | 主要功能无需悬浮或拖拽 | 选择菜单完成等价操作 |

## 9. 验证方法及决策依据

### 第一轮：检查信息架构

用同一份明确标识为演示的数据构建 A/B/C；覆盖 5、50、200 项资产，其中有共享、未知和同名对象。用完全相同的任务比较，轮换版本顺序，避免熟悉数据后的优势集中在后一个版本。

收集：任务成功率、找错账号次数、完成时长、往返次数、操作后对结果的理解；主观“喜欢哪版”另外记录。一次自测不推断普遍偏好。

### 第二轮：检查视觉品质

信息结构保持不变，比较两种排版风格；测试长中英文、不同屏幕和暗色环境。观察首眼注意力、扫描顺序、文字识别、信息密度感受。五秒理解是探索方法，不代替真实操作任务。

### 第三轮：检查完整管理流程

测试添加、批量归属、撤销、失败恢复、返回位置。基础记录 30 秒内保存、已知资产 10 秒内定位可作为初始目标，先取得基线再决定是否调整。

### 决策规则

- 关键任务经常失败或误判的方案先修正，不以审美投票覆盖功能失败。
- 两种方案效率相近时，优先选择更容易解释、更少设置且视觉更舒适的版本。
- 不强迫选出一个适用于所有任务的首页；可以用一个默认视图和少量清晰的替代视图。
- 发布前需要真实数据和真实用户验证；本轮尚未发生，不填写虚构测试结论。

## 10. 后续设计交付顺序

1. 一份贯穿所有版本的脱敏/演示资产样本。
2. 三种信息架构的低保真比较；先判断阅读顺序和寻找路径。
3. 优选结构的两种视觉方向，包含桌面和手机。
4. 搜索、添加、详情、整理、事件五条可操作路径及失败状态。
5. 根据任务记录修订的 UI 规范与交互说明。

这些才是下一阶段的主要产出。接口和技术选型仅补充原型必需的约束。无需用户先选一个好看的截图，也不把资料整理当成已经完成 UX 验证。
