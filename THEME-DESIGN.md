# 自定义色调 · 2026-09-25

后续修订：现已改为 OKLCH 明度与低色度表面，覆盖下述初版 HSL 配色。当前参数、官方来源和合成对比度验证见 [UI-COLOR-RESEARCH.md](UI-COLOR-RESEARCH.md)。

参考 Google Material 3 的 surface/container/on-surface 角色体系：
https://developer.android.com/codelabs/m3-design-theming

默认雾蓝，另提供鸢尾、玫瑰、暖沙、岩灰、鼠尾草及任意主色。由主色生成低饱和背景、中层类目、较浓卡片、深色文字及主按钮。使用自有 HSL 映射适配半透明玻璃，不是官方 HCT 动态配色算法。

浏览器和 Mac 原生顶栏都有「色调」入口。选择时即时预览，关闭恢复，确认才写入 state.theme（本地 JSON/浏览器本地存储）。原生窗口背景通过受限本地桥同步。

验证：预设预览、取消恢复、自定义 #ed7019 保存后刷新恢复通过。6 个预设及黑白红绿蓝极端输入的次要文字/不透明画板基底、白字/主按钮计算对比度最低约 4.58:1；这不是任意桌面背景下半透明材质的完整无障碍认证。旧数据无 theme 时使用雾蓝，非法颜色同样回退。
