# Assetboard：视觉、交互与需求资料地图

日期：2026-09-24 · 第一轮头脑风暴

**当前主界面方向**：大板按类别加入区块，内部放资产卡片；区块自适应并支持手动缩放，设可读性所需的最小限制；查看与编辑保持视觉连续。以 [大板与轻编辑设计](ASSETBOARD-BOARD-DESIGN.md) 为最新依据；以下布局候选为早期探索。

**优先级更新**：用户明确 UI / UX 是重中之重。请优先阅读 [UI / UX 研究](ASSETBOARD-UI-UX.md)。下文的“项目资产架优先”保留为首轮假设；新的研究将其与资产目录、常用入口视角共同验证，尚未定案。

## 1. 需求理解

让散落在不同平台里的数字资产，有一个能认得出来、找得到、看得清关系与日期的地方。

用户明确的困扰是：忘记在哪里有域名、服务器或会员，没有统一存放和标记日期的位置，且担忧收集与同步很费事。当前进一步明确：**先探索视觉和交互，不限定发布方式。** 因此本轮不以 SaaS、自托管或接口数量决定首页。

核心体验应回答五个问题：

1. 我有哪些东西？
2. 它们在哪里、用哪个账号？
3. 它们用来干什么，属于哪个项目？
4. 接下来有什么日期或费用需要注意？
5. 我能从这里直接去哪里处理？

“漂亮”的初步定义：排版克制、层次稳定、密度适当、对象易认；“方便”的定义：少跳转、少回忆、少录入、离开后回来仍找得到。以下均是待验证的设计建议。

## 2. 从具体使用瞬间推导界面

| 使用瞬间 | 用户想知道 | 最短交互 | 设计所需材料 |
|---|---|---|---|
| 想改某个网站 | 域名在哪、项目部署在哪 | 搜网站名 → 打开对应控制台 | 常用项目名、网址、入口 |
| 收到扣款提醒 | 这是什么、还要留吗 | 搜商户 → 看用途与归属 | 脱敏账单、别名、费用周期 |
| 隔几个月重新做项目 | 当时用了哪些服务 | 打开项目 → 看资产和一句备注 | 真实项目资产关系 |
| 想取消一个旧项目 | 有哪些资源、哪些还被其他项目使用 | 项目详情 → 查看共享依赖 → 去原平台处理 | 共享资源、合同/订阅关系 |
| 刚买新会员 | 先记住，不想填表 | 粘贴链接/一句话 → 保存 → 稍后补全 | 真实购买流程与日期 |
| 周末整理一下 | 哪些快到期、哪些归属不明 | 看近期事项与待整理 | 多种日期、缺失信息 |
| 只想快速进入后台 | 不想再找书签或登录错账号 | 常用入口 → 看账号提示 → 跳转 | 控制台链接、账号别名 |

## 3. 三种视觉方向

### A. 项目资产架——建议先深化

页面像一个整理好的工作台。每个项目是一块清晰分区，内部用对齐的紧凑行展示域名、部署、数据库等资源。共享服务和未归属资产也有自己的位置。

**视觉语言**：暖白或中性浅灰底色、深色正文、单一低饱和强调色、小而统一的服务商标识、轻分隔线。深色版本保持相同层次，不依赖发光效果。项目名称比平台 logo 更突出。

**交互**：项目展开/折叠；点资产名称开右侧详情；明确的“打开控制台”按钮负责外跳；搜索后保留分组；选择“归属项目”或拖动都可整理。

**优势**：最能解释资产用途，首页本身就是导航。**代价**：项目很多时需要折叠、置顶和筛选；共享服务不能硬塞进某个项目。

### B. 资产目录——适合经常按名称和平台查找

首页是一张精心排版的目录。每行展示名称、平台/账号、用途、下一事件，类型筛选固定在上方；可切换为缩略卡片，但结构一致。

**视觉语言**：留白集中在分组之间，行内紧凑；列稳定对齐；费用和日期采用等宽数字；横向尽量减少边框。

**交互**：键盘搜索和筛选优先，支持批量归属、批量收藏；详情与 A 共用；用户可以保存“我的域名”“共享工具”等视图。

**优势**：几十到几百条资源仍容易扫读。**代价**：可能像表格，需要强名称层级和用途提示；项目关系弱于 A。

### C. 数字资产地图——适合理解关系

围绕选定项目展开有语义的关系：域名指向部署，部署使用数据库，多个项目共享邮件服务。

**视觉语言**：固定布局或分层布局，连线标签明确；节点外观从属于信息。默认只展示相关的一小片关系。

**交互**：搜索聚焦节点、点击展开一层、查看入边与出边；提供同等信息的列表；手机直接转为关系列表。

**优势**：适合回答“它连着什么、停用会影响谁”。**代价**：维护关系有成本；节点多时拥挤；地图坐标不能取代可访问的导航。

**本轮推荐**：A 为首页，B 为“全部资产”，C 为项目详情中的可选关系视图。三个视角使用同一批数据，不要求用户分别维护。

## 4. 推荐首页的内容骨架

以下仅为信息位置草图，项目、数量和日期均为虚构占位，不代表用户真实资产。

```text
Assetboard                    搜索资产、项目、账号…      ＋ 添加

总览   全部资产   时间线                         显示：舒适 / 紧凑

需要留意  2 项     下次事件：example.dev · 12 天后到期

项目与资产                                      近期事项
┌ 写作项目                          打开项目 ┐   10/06 域名到期
│ example.dev      域名      注册商 A         │   自动续费：未知
│ writing-web      部署      平台 B           │   [查看来源] [去处理]
│ writing-db       数据库    平台 C           │
│ 3 项资源 · 1 项日期待补全                    │   待整理
└────────────────────────────────────────────┘   2 项未归属
                                                1 项可能重复
┌ 实验项目                                   ┐
│ prototype.dev    域名      注册商 A         │
│ prototype        仓库      平台 D           │
└────────────────────────────────────────────┘

共享工具    编辑器会员 · 邮件服务 · 开发者账号
未归属      旧域名 · 测试服务器

数据更新：部分来源 20 分钟前更新；1 个来源需要重新连接
```

首页层次：搜索与添加 → 少量重要事项 → 项目和资产 → 共享/未归属 → 来源更新概况。

不强迫首页顶部展示四块大统计卡。资产数量可出现在导航旁；只有费用覆盖足够清楚时才展示汇总；连接器管理放到来源设置。

## 5. “优雅”需要落实到的细节

| 维度 | 建议规则 | 需要比较的版本 |
|---|---|---|
| 信息密度 | 首屏能看到多个项目和实际资产；让用户选择舒适/紧凑 | 两种行高，而非两套功能 |
| 颜色 | 常态中性，提醒只标相关字段；颜色同时配文字 | 浅色、深色各一版 |
| 字体 | 清晰中文正文；域名与数字不混淆；长名称可截断并展开 | 长中英文混排样本 |
| 图标 | 类型图标帮助理解、平台标识帮助认出，不抢占面积 | 有/无平台 logo 的对照 |
| 对齐 | 名称、平台、日期具有稳定位置 | 项目内列表与资产目录 |
| 动效 | 展开和侧栏过渡轻微，遵循减少动画设置 | 无动效也完整可用 |
| 操作显露 | 主要入口始终可见；次要操作放菜单 | 鼠标、键盘、触屏 |
| 稳定性 | 同步和日期变化不让卡片突然跳位 | 新资产到来、排序刷新 |
| 手机 | 单列分组、详情独立页、筛选底部面板 | 390px 宽真实回读 |
| 私密展示 | 可隐藏金额、邮箱、IP；演示模式明确标识 | 共享屏幕时不露私人字段 |

不要让每张卡片都长成相同的大方块。项目、域名、服务器和订阅可以共用排版语法，同时保留不同的信息重点。

## 6. 核心交互需要先画全

### 搜索与导航

- 支持名称、别名、域名、项目、平台和账号别名检索；不要求先选类型。
- 结果显示“是什么、属于谁、在哪里”，而不只有名称。
- 可用 Cmd/Ctrl+K，但有明确的可点击搜索入口。
- 点名称查看信息，点外跳图标进入原平台；不要让整行承担两种含糊动作。
- 返回时保留查询、筛选和滚动位置。

### 添加与整理

- “粘贴网址 / 写一句话 / 手动添加 / 导入”共享一个入口。
- 最少可以只保存名称，其他字段稍后补；未知日期不能阻塞保存。
- 从文字提取的信息先展示预览，再确认；规则能处理的字段不必使用 AI。
- 分配项目支持选择和拖动；多选批量操作并可撤销。
- 可标“共享”“暂时不知道”“忽略此候选”，避免未完成整理成为焦虑来源。
- 别名与“保留原因”应比长备注更容易填。

### 详情

- 首屏显示用途、平台账号、直接入口、下一事件。
- 下方再展开费用、关联资源、来源、最近变化与备注。
- 域名分别显示注册商与 DNS；服务器可显示区域与运行服务；订阅突出计费周期。
- 明确区分“在 board 归档”和“去服务商取消”；本地归档不等于停止扣费。

### 日期与提醒

- 区分到期日、扣款日、取消截止日、试用结束日、优惠结束日；同一资产可有多个事件。
- 优先显示“下一件事”，完整历史放时间线。
- 提醒可稍后、已处理、修改日期；“已处理”需说明记录了什么，不能暗示服务商已续费。
- 同一事件更新后合并提醒；断连单独提示，不让每条资产都出现红色警报。
- 提醒渠道暂不锁定，后续比较应用内、日历订阅及可选消息通知。

### 首次使用

先给一个可理解的示例视图，可清空；再允许添加少量真实资产。连接平台是渐进增强，用户不必先填完账号和项目体系。

成功点是“我已经能从这里找到一件之前容易忘的东西”，而不只是连接成功。

## 7. 界面需要哪些资料

### 应先准备的最小样本包

建议 15–25 条代表性记录、3–5 个项目。数量是设计取样建议，并非产品门槛。优先真实且脱敏的样本；尚无授权或数据时可用明确标记的演示数据。

| 材料 | 至少包含什么 | 用来判断什么 | 当前情况 |
|---|---|---|---|
| 资产样本 | 名称、类型、平台、用途、入口、归属 | 卡片和目录是否好读 | 未取得真实样本 |
| 项目关系 | 一个完整项目、一个共享资源、一个旧项目 | 分组是否符合实际心智 | 未取得 |
| 账号区别 | 同平台两个账号/团队的别名 | 是否能避免跳错后台 | 未取得 |
| 日期样本 | 自动续费、手动续费、未知日期、试用结束 | 事件标签及提醒优先级 | 未取得 |
| 费用样本 | 月付、年付、用量、买断、免费、未知 | 汇总是否误导 | 未取得 |
| 常用入口 | 官网、项目控制台、账单、域名管理 | 外跳如何减少寻找 | 未取得 |
| 最近遗忘经历 | 发生了什么、当时去哪里找、多久找到 | 用真实任务验证交互 | 对话有困扰，无具体时长 |
| 视觉参考 | 喜欢/不喜欢的密度、字体、明暗风格 | 避免“优雅”停留在形容词 | 已有三个研究参照 |
| 显示环境 | 常用屏幕、手机使用频率、键盘习惯 | 默认密度和响应式规则 | 未确认 |

这是一份材料地图，不要求用户先自行填完。下一阶段可先做合成样本探索，再在获得范围授权后读取指定平台或导出文件，逐步替换。

### 必须出现的边界样本

1. DNS 在 A、注册商在 B 的同一域名。
2. 同一平台的两个账号，以及名称相似的项目。
3. 同一资产被两个项目共享。
4. 尚无项目归属的资源。
5. 没有日期、没有金额的记录。
6. 已取消续费但仍可用到月底的订阅。
7. 已归档项目关联仍付费资源。
8. 年付费用、用量账单、不同币种并存。
9. 同步失效但之前的数据仍存在。
10. API 和账单分别发现同一资产，可能重复。
11. 超长域名、中英混合名称、没有 logo。
12. 首次空白、5 项、50 项和 200 项资产的界面。

### 现在不需要收集

密码、完整银行卡号、恢复码、所有邮箱正文、无关仓库代码。账号别名足以支持早期设计；若有凭证，仅在实际接入阶段使用专门的安全配置流程。

## 8. 信息模型：藏在简单界面下面

| 对象 | 关键资料 | 设计理由 |
|---|---|---|
| 项目 | 名称、用途、生命周期、常用入口 | 回答“为了什么” |
| 资产 | 类型、外部标识、名称、平台、账号、使用状态 | 不依赖名字判断同一对象 |
| 关系 | 连接双方、关系类型、来源、确认状态 | 支持共享，区别“依赖”和“属于” |
| 费用 | 金额、币种、周期、实际/估算、覆盖范围 | 避免一份账单算多次 |
| 事件 | 类型、日期、时区、来源、处理状态 | 到期与扣费可不同 |
| 来源 | 平台/导入/手动、抓取时间、覆盖范围、错误 | 解释这张 board 知道什么 |
| 账号 | 平台、账号/团队别名、管理入口 | 同平台多个身份可辨认 |

补充规则：

- 同一平台、账号、资源类型和外部 ID 作为去重依据；域名归一化辅助跨源匹配，不盲目合并。
- 来源提供事实，人补充用途；同步不能清掉用户的项目归属和备注。
- 一个 API 本次没返回某资源，可能是权限、分页或故障；不立即判定删除。
- “在用”是使用状态，“在线”需要独立监测证据，“同步正常”只证明连接可用。
- 未知 ≠ 免费；过期信息 ≠ 当前状态；无活动 ≠ 可以删除。
- 展示原币费用；换算需要汇率来源和时间。年付月均不能当成下月扣款金额。
- 来源覆盖要可见，例如“已连接 2 个账号；部分费用待补全”，不写“你的全部资产已找到”。

## 9. 自动化与开源留作设计边界

平台文档核查见 [研究来源](ASSETBOARD-RESEARCH.md)。建议路径为：少量手动/导入就能用 → 按真实资产分布选首批直连 → 再研究账单候选发现。

GitHub、Cloudflare、Vercel 是可研究的首批连接对象，不等于已经支持。Gmail 全量读取不作为视觉原型的前置条件。服务器、数据库、支付记录等平台需逐项调查，不承诺一键全接。

如后续进入开发，应研究凭证存放、只读权限、断开与删除、数据导出、备份恢复和连接器失败隔离；如决定开源，再选择许可证及第三方代码复用方式。本轮不锁框架、数据库、部署或商业模式。

## 10. 可取舍的想法池

评分为设计者主观比较：新颖性/可行性均为 1–5，不是用户证据。

| 想法 | 新颖性 / 可行性 | 本轮建议 | 理由 |
|---|---|---|---|
| 项目资产架 | 3 / 5 | 核心 | 用途与入口放在一起 |
| 全局速查与直达 | 2 / 5 | 核心 | 解决“在哪里” |
| 下一事件条 | 2 / 5 | 核心 | 让日期进入日常视线 |
| 未归属收件箱 | 3 / 4 | 核心 | 不强迫一开始整理完 |
| 一句话保存 | 3 / 4 | 核心候选 | 降低随手记录成本 |
| 一句保留原因 | 3 / 5 | 优先 | 减少重复回忆 |
| 舒适/紧凑切换 | 2 / 5 | 优先 | 适配规模和屏幕 |
| 项目关系地图 | 3 / 3 | 次级视图 | 依赖图有用，但不适合所有首页 |
| 最近变化摘要 | 3 / 3 | 后续 | 需要可靠差异与来源 |
| 停用项目检查单 | 3 / 3 | 后续 | 应先有可靠共享关系 |
| 邮件候选发现 | 3 / 2 | 后续研究 | 权限、识别和去重成本 |
| 自然语言问资产 | 3 / 3 | 可选 | 不能替代搜索与稳定导航 |
| 自由拼装组件 | 2 / 3 | 暂缓 | 容易让用户先设计自己的工具 |
| 全自动停服/取消 | 2 / 1 | 暂不进入 | 当前需求是查看和方便处理 |

## 11. 原型应怎样验收

以下是建议目标，尚未测试：

- 首次看到首页，5 秒内理解资产按什么组织、哪里有需处理事项。
- 已知名称的资产，10 秒内找到平台、账号与入口。
- 未知金额、过期同步、待确认关系都能被识别，不被误读成正常或免费。
- 新增一条基础记录不超过 30 秒；允许日后补全。
- 从列表查看详情再返回，不丢位置；主要操作可完全通过键盘完成。
- 在桌面和手机上实际检查长名称、50/200 项、空白、错误状态，不只看理想截图。
- 让使用者完成“找后台、查下次日期、找旧项目依赖、归属一条新资产”四个任务，记录耗时、误点、求助和主观理解。
- 之后才验证连续使用能否减少遗漏和维护负担，不从一次漂亮演示推断需求或付费意愿。

## 12. 本轮结论与下一步

优先把 **A 项目资产架** 和 **B 资产目录** 用同一份样本画成可比较版本，先验证“舒服地扫一眼”和“快速找到入口”。选定主视图后再补添加、详情、整理和错误状态。关系地图作为进一步探索。

当前未确认：实际资产构成、首屏密度偏好、深浅主题、主要设备与真实操作路径。当前已明确：视觉和交互优先，不限定发布方式。
