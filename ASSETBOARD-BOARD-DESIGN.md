# Assetboard：可编排大板与轻编辑

2026-09-24 · 当前设计方向。用户确定方向；下述尺寸、操作细节与默认布局为待验证建议。

最新视觉修订：统一毛玻璃材质，每层结构只保留一种底色；取消类别彩色底板、卡片内彩色衬底、数量和底部统计。大板贴合窗口，区块零间隙，以细分隔连接；仅保留顶部功能栏。Mac 使用原生背景毛玻璃，卡片图形降低饱和度，保留对象辨识。

圆角修订：区块保留 14px 轻圆角，资产交互区域 10px 圆角，维持零间距。去掉标题前重复类别图标、提醒时钟和调整布局按钮的装饰符号；折叠、添加、更多、外跳等操作标识保留。“查看全部”仅在固定高度隐藏了资产时出现，用文字明确含义。资产主体图形保留。

最新间距修订：用户确认结构符合预期，但希望各结构之间有 3–5px 全等微间距。采用统一 4px：区块间、卡片间、大板外沿一致；覆盖此前零间距建议。区块宽度与拖动缩放计算扣除间距，避免意外换行。圆角与毛玻璃保持不变。

最新材质修订：用户要求液态玻璃视觉并强调大板、类目区块、资产卡片三层层次。采用统一中性色：大板为柔和底层，类目为磨砂玻璃与连续边缘，卡片为更清晰、更亮的独立玻璃表面，包含低强度指针高光。类目与卡片间、卡片之间保留 4px 微间距。通过透明度、模糊强度、边缘高光和轻阴影区分层次，不恢复多种类别底色。为 CSS 玻璃视觉与原生毛玻璃结合，不声称使用 Apple Liquid Glass 原生组件。

边缘处理：按用户提出的阴影/毛边方向，采用柔化高光边与环境阴影，不使用粗糙锯齿纹理。类目阴影较宽较淡，卡片阴影较近，硬白描边减弱；内部四周及卡片间统一 4px。

透明度修订：用户认可结构和层次，但反对白色大色块。类目填充降至 8–16% 透明色，卡片填充降至 8–20%，悬浮态仍保持通透；大面积指针高光同步减弱。保留边缘和阴影，不降低文字透明度。详情和弹窗保留较高的半透明底以确保阅读。

展示层级、折叠摘要、类别完整视图及资产详情的具体方案见 [展示与展开交互](ASSETBOARD-DISCLOSURE.md)。

最新交互修订：内部资产卡片收成独立的「透明底图标 + 名称」，仍可点开详情；类目随紧凑卡片网格自适应宽高，最多两列，不压成整条横栏。再次展开恢复原设置宽高。留白指透明空间：PNG 四周透明，卡片取消面填色，保留玻璃模糊、边缘与阴影。搜索暂时展开卡片，清空搜索恢复紧凑状态。类目背景模糊 52px、卡片 28px。新增资产继续使用首字替代图形。

## 首要表达原则：图形化

2026-09-25 材质层级修订：覆盖上一版卡片完全透明底。大板最透明，类目使用 30–40% 同色系雾色，卡片使用 46–60% 更浓的浅灰绿玻璃。边缘与阴影逐层加强，资产名称比类目标题和次要信息更清晰。PNG 本身及插画区域仍保持透明留白，不增加图标底板。

用户明确强调“图形化才是最重要的”。大板应首先通过图形、空间分组和对象形象让人认出资产，再用短文字提供精确含义。此前偏向文字清单的卡片示意只用于说明字段，不代表最终视觉方向。

- 区块用类别图形、空间边界和一致的视觉风格建立分区；标题辅助识别。
- 卡片以资产形象为视觉主体，例如域名的网站标识/缩略预览、服务器的设备符号、订阅的软件标识；没有图片时提供统一且可辨认的替代图形。
- 图形只表达有依据的事实：网站缩略图是预览而非在线证明；服务器图形不伪装成真实设备照片。预览按需获取，私人控制台不默认截图或外传。
- 日期可用小型日历标记和相对时间；连续指标仅在具有明确总量、起点和当前值时使用进度条。未知值保留可见标签，不编造完整状态。
- 色彩、形状、图标与简短标签共同表达状态，颜色不能单独承担含义。服务商品牌色与事件警示色分工清楚。
- 类别、平台和资产身份有层级：类别在区块识别，平台在卡片识别，具体名称用于区分同平台的多个资产，避免满板重复 logo。
- 资产卡片保留名称和最重要的事实，更多字段进入详情；避免把表格的每一列逐行塞进卡片。
- 折叠后的区块保留图形识别线索与数量/重要事件；展开延续原有图形和空间位置，减少形态突变。

图形化优先不等于增加装饰图表。每个视觉元素都应帮助识别对象、理解关系、比较状态或执行动作。实际图形样式、缩略图比例和信息密度需通过视觉原型比较，尚未定案。

## 1. 本轮收敛

用户要求：“最好是一个大板划分区域，有点类似于 iOS 小组件编辑那种感觉，然后编辑态放在和查看态 UI 差异调小”。

最新补充：用户希望区域大小自适应、设置最小限制，同时支持放大缩小；按类别加入区块，区块里放资产卡片。

主界面采用“大板 → 类别区块 → 资产卡片”三层结构。域名、服务器、订阅等类别是默认组织方式；项目保留为资产的关联信息与筛选维度。查看与布局编辑共用内容、位置、尺寸和视觉样式。取消此前仅提供小中大固定尺寸的建议。

借鉴 [Apple 小组件添加与编辑](https://support.apple.com/en-sg/118610) 的选择、配置和移动方式。本站的低差异编辑、栅格和保存行为是自己的设计建议，不表示完全复刻 iOS。

## 2. 板、区域与资产

- **板**：用户的整个工作台，主要纵向滚动，原型先不引入无限画布的缩放和平移。
- **类别区块**：按类别加入的容器，例如域名、服务器、订阅、数据库；标题、数量与添加入口属于区块。
- **资产卡片**：区块内部的一条实际资产记录，显示名称、平台/账号、用途和重要日期。卡片根据所属类别突出不同字段。

移动区块只改变布局，不改变资产归属。移除区块只撤掉这个视图，不删除资产。卡片在同区块内拖动只排序；跨类别拖动涉及分类变更，首轮不默认启用，先通过明确的修改类别操作完成。项目关系独立于卡片在板上的位置。

## 3. 大板的默认起点

提供一个直接可用、能够修改的布局，用户不必从空画布开始设计。

```text
我的资产                      搜索…   ＋ 添加区块   调整布局

┌───────────────────────────┐ ┌───────────────────┐
│ 域名 · 3             ＋   │ │ 服务器 · 1    ＋   │
│ ┌─────────┐ ┌─────────┐  │ │ ┌───────────────┐ │
│ │ 资产 A  │ │ 资产 B  │  │ │ │ 服务器卡片    │ │
│ └─────────┘ └─────────┘  │ │ │ 平台 / 用途   │ │
│ ┌─────────┐              │ │ └───────────────┘ │
│ │ 资产 C  │              │ └───────────────────┘
│ └─────────┘              │
└───────────────────────────┘
┌───────────────────────────────────────────────┐
│ 订阅 · 3                                  ＋  │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│ │ 编辑器会员 │ │ 设计工具   │ │ 邮件服务   │   │
│ └────────────┘ └────────────┘ └────────────┘   │
└───────────────────────────────────────────────┘
```

这是位置示意，不是像素规格，也不是用户真实资产。添加区块时先选择类别，立即展示该类别已有资产；在区块中添加资产时预填类别。空区块保留简洁的添加入口。全局搜索覆盖全部资产，并有“全部资产”入口，避免板上没放就找不到。

## 4. 查看与编辑只增加一层操作提示

| 元素 | 查看时 | 调整布局时 |
|---|---|---|
| 卡片内容、字体、颜色 | 正常展示 | 完全保留 |
| 位置和尺寸 | 已保存布局 | 进入时不变化，操作后才变化 |
| 边界 | 轻边框或留白分隔 | 边框稍加强，选中项更清楚 |
| 标题栏 | 类别、数量及更多菜单 | 增加拖动手柄、尺寸控制 |
| 添加入口 | 全局添加区块，区块内添加资产 | 保持原位置与含义 |
| 顶部工具 | 调整布局 | 完成、撤销；布局保存状态 |
| 内容操作 | 查看、外跳、修改资料 | 原有行为保持；拖动只从手柄开始 |

工具放进预留空间或覆盖层，出现时不挤动正文。避免全板持续抖动、大面积遮罩、把真实数据替换成占位表单。

状态差异可以小，但必须可辨：顶部明确写“调整布局”，当前组件有焦点边框。用户应始终知道拖动的是布局，而不是业务数据。

## 5. 编辑分成三个尺度

### 修改资产资料：随时就地进行

无需进入布局编辑。查看详情后即可改名称、用途、日期或项目归属；短字段可用小型浮层，复杂资料使用侧栏。保留所在区域与滚动位置。

### 配置一个组件：局部完成

组件菜单中的“设置”打开轻量浮层，选择展示范围、排序、可见字段；底层仍看到实际组件。改标题在标题处完成。预览不等于保存，取消应恢复原配置。

### 调整整张板：轻编辑态

明确按钮进入；触屏长按可作为辅助入口，不作为唯一入口。移动、改尺寸、添加和移除组件都在当前板上完成。

布局改动建议自动保存并提供撤销，“完成”仅退出编辑。保存失败时保留本次布局，显示“布局尚未保存”与重试，不能显示虚假的已保存状态。Esc 退出编辑不默认回滚已保存修改；此语义需在原型中验证。

## 6. 排列和尺寸的约束

### 自适应是默认行为

- 区块宽度适应大板的可用空间，内部资产卡片自动换列；默认高度随卡片行数增长。
- 区块最小宽度由“一张可读卡片 + 两侧留白”决定；最小高度由“标题栏 + 一行卡片 + 留白”决定。空区块使用独立的较低最小高度。
- 最小值约束可操作内容，不固定照搬某设备上的像素值。窄屏使用适配后的单列卡片布局，不能因桌面最小宽度而横向溢出。
- 卡片间距、字体和主要操作尺寸保持稳定；区块变窄时减少列数，不把整块内容按比例缩小。

### 手动调整表达用户偏好

- 轻编辑态下拖边缘或角落可连续调整区块大小；可以轻微吸附对齐，但不强制只有小中大几档。
- 仅改宽度时，高度继续自适应。用户显式改高度后，视为指定展示高度，避免下一次内容刷新立即覆盖手动设置。
- 提供“恢复自适应高度”；宽度在窗口变窄时受可用空间约束，保留宽屏偏好以便恢复。
- 固定高度容不下全部卡片时，显示完整卡片行及“已显示 X / N · 查看全部 N 项”，进入类别完整视图，不修改大板布局。该操作与收起/展开区块、手动调整大小明确区分，详见展示与展开交互方案。
- 放大后增加可见列数、行数或给内容更多空间，避免无限拉宽单张卡片；卡片的舒适最大宽度待原型验证。

### 调整过程与布局稳定

- 拖动/缩放显示落点和邻近区块让位预览；松手提交布局。不重叠，不无故改变其他区块顺序。
- 宽度改变引起卡片换列，是有意的局部重排；周边只做必要让位。
- 主动添加资产时，自动高度区块可增加一行并推开下方区块；后台新数据到来时先提示新增，不在用户阅读中突然改变布局，应用更新后再重排。
- 拖动有等价位置操作；尺寸也有可键盘操作的调整控件，触屏使用较大热区。

## 7. 区块统一结构，资产卡片按类别表达

| 类别 | 卡片核心显示 | 按需补充 |
|---|---|---|
| 域名 | 域名、注册商、到期日 | DNS 平台、关联项目 |
| 服务器 | 名称、平台、用途 | 区域、配置、费用 |
| 订阅 | 服务名称、周期费用、下次扣款 | 账号、自动续费状态 |
| 数据库 | 名称、平台、关联项目 | 环境、管理入口 |
| 软件授权 | 软件名称、授权类型 | 购买账号、维护到期日 |

区块使用轻底色或间距表达分组，内部卡片承担对象识别和点击；避免外层、内层同时使用厚边框和强阴影。卡片共用名称、用途、平台、关键事件的阅读顺序，类别决定字段重点。项目标签支持跨类别查找。近期事项等汇总功能暂作为辅助入口，不挤占“类别区块 + 资产卡片”的核心结构。

## 8. 手机与稳定性

桌面空间编排在手机上按稳定顺序转换为单列或适用的双列；宽组件占满可用宽度。跨设备调整需要分别保存布局或有明确继承规则，避免手机整理破坏桌面布局。

暂不引入嵌套区域编辑、多块无限画布与组件自动轮播，先验证一张板的查找与整理体验。用户需要时再扩展。

## 9. 本方向的验证重点

1. 查看/编辑两张画面对比：除了工具提示，原有内容不跳位、不消失。
2. 用户能辨认正在调整布局，拖动不会触发外跳或误改资产归属。
3. 添加、移动、改尺寸、移除、撤销均能在当前板完成。
4. 移除区域后资产仍在全局搜索中，用户理解这个区别。
5. 少量和大量资产都能有效浏览；板只展示重点但完整目录始终可达。
6. 更新、保存失败和窗口变窄不会破坏用户布置。
7. 连续缩放区块时，卡片能正确换列；到最小边界后内容仍完整可用。
8. 改宽保持自动高度，显式改高后保留用户选择；可恢复自适应。
9. 区块按类别载入对应资产，区块中添加资产预填类别；移动布局不改变分类。

下一步原型重点：同一张板的查看态、调整态、拖动态、组件设置态，而不是继续比较互斥首页。当前只完成设计收敛，尚未制作或验证这些原型。
