# 展示、折叠与展开交互

2026-09-24 · 基于已确定的“大板 → 类别区块 → 资产卡片”。以下为首轮设计方案，未做交互原型或用户验证。

**最高视觉优先级**：图形化。以下文字草图只说明信息与行为，不是最终 UI 外观。卡片应以可识别的资产形象为主，区块折叠后仍保留图形线索，见 [图形化原则](ASSETBOARD-BOARD-DESIGN.md)。

## 1. 核心判断

默认应该看得到资产，而不是满屏收起的类别。折叠用于主动减少不常看的内容；详细参数用于按需查阅。识别资产、判断近期事项和进入后台，不应要求逐层展开。

四个动作各自承担一种含义：

| 动作文案 | 用户目的 | 呈现形式 | 是否修改布局 |
|---|---|---|---|
| 收起 / 展开区块 | 暂时少看一个类别 | 原地收成摘要条 / 恢复原大小 | 暂时收高，保留展开尺寸 |
| 查看全部 N 项 | 查找这个类别中的更多资产 | 当前工作区进入该类别的完整视图 | 不改大板尺寸 |
| 查看详情 | 了解或修改一项资产 | 桌面侧边详情，手机独立详情页 | 不改大板布局 |
| 调整大小 | 长期改变该类别占用的空间 | 在大板上拖动边缘 | 保存用户尺寸偏好 |

不把四种动作都叫“展开”。不再使用含糊的“展开全部”来同时表达增高区块和查看所有资产。

## 2. 区块的显示与收起

### 默认展开

新加入的区块默认展开，显示标题、资产总数、添加入口和资产卡片。没有资产则给出一处简洁的添加提示，不默认折叠空区块。

标题与箭头组成一个可点击的折叠按钮；右侧添加、更多菜单、编辑态拖动手柄分别独立。点击卡片或标题栏空白不触发折叠。

### 收起后依然留下可判断的摘要

```text
▶ 域名  12                  2 项即将到期       ＋  ⋯
```

至少保留类别、总数、折叠指示。若存在需处理事件，保留一种最重要的摘要；空间不足时可换成两行。没有事件就不硬塞“全部正常”。没有检测或日期数据，不能推断没有风险。

图形化处理建议：摘要条由类别图形、少量资产缩略标识、总数和关键事件标记构成；缩略标识只作预览，不变成一串过小的点击目标。事件同时有短文字。展开时这些识别线索与卡片中的视觉形象一致。

事件摘要可单独点击进入该类别的对应筛选结果，不必先展开区块再找。这里是明确的独立按钮，不嵌套在折叠按钮里。

### 折叠与布局的关系

- 收起时保留宽度、顺序和展开前的高度模式；高度使用摘要所需的独立最小值。
- 下方内容作必要的向上让位，但不跨列重新洗牌。重新展开恢复原宽度及自动/手动高度设置，下方内容让位。
- 折叠不覆写用户保存的展开尺寸；不能下次恢复成一条很矮的区域。
- 记住用户主动收起的类别；不根据“很久没点”自动折叠，不因进入编辑态而全展开。
- 折叠区块在布局编辑中仍可移动、调整宽度；高度缩放不可用。展开后再调整展示高度。
- 保持被点击标题的屏幕位置尽量稳定，避免收起后视线跳到其他区域；由原型检查实际滚动锚定。

## 3. 资产卡片：哪些直接展示

卡片的任务是认出对象并作初步判断。建议统一四个位置，而非强制统一所有字段。

```text
example.dev                         ⋯
注册商 A · 个人账号
项目：写作工具
10 月 06 日到期 · 12 天后       打开管理 ↗
```

日期和名称为虚构示例。

| 层级 | 默认显示 | 原因 |
|---|---|---|
| 身份 | 名称 / 域名，类型识别线索 | 必须不用点开就认出对象 |
| 位置 | 平台；多账号时显示账号别名 | 防止同平台同名混淆 |
| 用途 | 一句用途或项目标签 | 减少重新回忆 |
| 下一件事 | 关键日期或该类别最有用的事实 | 帮助判断需不需要行动 |
| 主操作 | 一个明确的管理入口 | 无需先打开详情再外跳 |

各类别的第四行可以不同：域名显示到期；订阅显示金额/周期及下次扣款；服务器显示用途相关的区域/配置或已知续费信息；数据库显示环境或关联项目。没有在线监测证据就不放绿色在线点。

名称、平台、重要事件不因区块缩窄而被藏起来。次要用途可截断一行，但完整内容能在详情中读取；触屏不依赖悬浮提示。

## 4. 卡片哪些不在默认面上展示

以下进入资产详情：完整 IP、外部资源 ID、完整 DNS/配置、购买历史、完整备注、历史账单、数据来源与更新时间细节。

重要未知信息例外：到期日未知应在需要日期的卡片上直接写“到期日待补充”；来源已过期影响当前判断时显示简短提示，详细原因再展开。

卡片默认不做独立翻面或高度展开。逐张展开会导致同一区块的卡片高低参差，缩放与排序也变难。需要更多空间时使用同一套详情面板。

资产卡片的名称/主要内容区可打开详情；管理链接和更多菜单有独立点击区。实现时不得把多个按钮嵌在一个大按钮内。拖动只通过布局编辑中的指定手柄触发，日常点击和选文字不拖动卡片。

## 5. 资产详情怎样展开

### 桌面：侧边详情面板

- 从右侧打开，保留大板位置，不把整张板挤窄造成卡片换列。面板覆盖右侧部分工作区，宽度上限与窄屏切换点由原型验证。
- 一次显示一个资产。足够宽的屏幕上可继续选其他卡片，更新同一面板；有未保存编辑时先处理保存/放弃。
- 顶部固定对象名称、平台账号与打开管理入口。用途、项目、日期及费用紧随其后。
- 参数、来源记录、历史作为次级折叠节；有内容的用途和重要日期不先折叠。
- 简短内容少时直接展示，不为少数几行加手风琴；不出现折叠套折叠。
- 关闭返回触发它的卡片并保留滚动。非模态桌面面板不困住键盘焦点；需要清楚的进入、返回与关闭路径。

### 手机：独立详情页

保持同样的信息顺序，用返回回到原区块和位置。长表单和多段信息不塞进矮小底部弹层。小型操作菜单可用底部面板。

### 编辑资料

查看与编辑使用相同字段顺序；点具体字段附近的编辑入口后原位变成输入控件，并提供保存/取消。不要整页突然换成另一套表单。复杂字段可展开局部编辑区。

## 6. 一个类别有很多资产时

自适应区块继续尊重“内容决定高度”，不擅自设置“最多六张”之类的隐藏上限。资产很多时会变长，这是需要在原型里检验的真实代价。用户可主动收起或指定展示高度。

手动固定高度的区块：显示放得下的完整卡片行，底部明确写“已显示 6 / 18 · 查看全部 18 项”。预留底部入口空间，避免它盖住卡片；隐藏部分有重要事件时，在区块标题保留摘要。

点击“查看全部”后，当前工作区进入该类别完整视图：卡片沿用原样，顶部提供类别内搜索、筛选和“返回大板”。这是临时查看上下文，不是把原区块永久撑满屏幕。关闭或返回恢复原区块高度和滚动。

在完整类别视图中打开卡片，仍使用同一套资产详情。返回链只有“类别视图 → 大板”与“资产详情 → 来处”，不层叠多个模态弹窗。

首轮不采用各区块内部独立滚动，避免大板、区块、详情出现三重滚动；如大量资产测试证明固定高度场景需要局部滚动，再做单独对比。

## 7. 搜索与折叠的协作

- 全局搜索覆盖收起区块及未展示卡片，不只查当前看得见的部分。
- 搜索结果直接展示资产，并标注类别、平台与用途；点击可直接打开详情。
- 搜索不永久改变大板的折叠状态、尺寸和排序，退出后回到原布局。
- “在板上定位”才临时展开对应区块并高亮目标；若目标不在固定高度可见范围内，进入类别完整视图定位。提示实际去向，避免跳到一个看不见目标的区块。
- 临时展开与用户主动折叠分开记录，结束定位后可返回之前的显示状态。

## 8. 边界行为

| 情况 | 行为 |
|---|---|
| 收起区块出现新到期事件 | 更新摘要，不擅自展开或改变排序 |
| 新资产加入固定高度区块 | 更新总数，说明已添加并提供查看；不假装一定在可见区域 |
| 区块缩小 | 换列，保留身份和事件信息；固定高度显示完整行及计数 |
| 展开详情时缩小窗口 | 转为窄屏详情形式，保留所选对象和未保存输入 |
| 收起时焦点仍在区块内部 | 焦点回到区块折叠按钮，隐藏区域不再可 Tab 进入 |
| 详情保存失败 | 保留输入与错误，不关闭面板 |
| 快速重复点击收起/展开 | 以最新目标状态为准，动画不得留下错误高度 |
| 进入布局编辑 | 维持现有展开状态，关闭容易遮挡拖动的局部菜单；未保存编辑先处理 |

## 9. 交互依据与验证

参考 [NN/g 桌面折叠的取舍](https://www.nngroup.com/articles/accordions-on-desktop/)：折叠会增加发现和点击成本，因此一览场景不能把核心内容默认全部隐藏。参考 [Carbon Accordion](https://carbondesignsystem.com/components/accordion/usage/) 的渐进披露，以及 [W3C Disclosure Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/) 的按钮与展开状态语义。以上为原则参考，不能替代本产品验证。

原型需至少覆盖：展开大板、收起摘要、固定高度溢出、类别完整视图、资产详情、字段编辑六个画面，以及关闭返回。

验证任务：

1. 不打开任何详情，认出资产并找到正确管理入口。
2. 类别收起时，仍发现其中有需要处理的事件。
3. 查看被隐藏的第 18 项，再回到原布局。
4. 收起后再展开，手动高度与自动高度分别正确恢复。
5. 修改一项日期、保存失败后重试，布局与输入均不丢失。
6. 手机和纯键盘完成以上任务，不依赖 hover、拖动或小箭头精确点击。

验收依据是任务完成、误操作和返回上下文是否准确；本轮只完成方案，不声称这些验证已经通过。
